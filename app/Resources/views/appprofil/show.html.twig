{% extends 'base.html.twig' %}

{% block body %}
    <h1>Appprofil</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appProfil.id }}</td>
            </tr>
            <tr>
                <th>Key</th>
                <td>{{ appProfil.key }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ appProfil.value }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('appprofil_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('appprofil_edit', { 'id': appProfil.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
